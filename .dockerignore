# Dependencies and build output — reinstalled / rebuilt inside the image
node_modules
.next
out
build
lib/generated

# Version control and CI
.git
.github
.gitignore

# Infrastructure — a separate project, not part of the app image (ADR-008)
cdk

# Local AI-workflow, planning, and editor artifacts
.claude
.planning
.superpowers
.vscode
.idea
.screenshots
.playwright-mcp

# Tests and coverage reports
coverage
playwright-report
test-results

# Synthetic dev fixtures. `npm run seed` wipes the core tables and re-creates
# them from fixtures; nothing deployed ever needs it, so keep it out of the
# image entirely rather than relying on the in-script NODE_ENV guard alone.
seed

# Environment files — never copy credentials into an image
.env
.env.*

# Docker and Compose
Dockerfile
.dockerignore
docker-compose.yml

# Logs and OS noise
*.log
npm-debug.log*
.DS_Store
