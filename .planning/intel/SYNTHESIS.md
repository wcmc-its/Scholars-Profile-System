# Synthesis Summary

Entry point for downstream consumers (`gsd-roadmapper`). Generated by `gsd-doc-synthesizer` from the per-doc classifications in `classifications/` and the source documents in `.planning/source-docs/`.

---

## Doc counts by type

| Type | Count | Sources |
|---|---|---|
| ADR | 1 (containing 8 decisions) | `phase-1-design-decisions.md` |
| SPEC | 2 | `design-spec-v1.7.1.md` (precedence 1, treated as locked UI/UX contract), `functional-spec-phase-1.md` (precedence 3) |
| PRD | 1 | `charter.md` (precedence 9, intentionally downplayed by user) |
| DOC | 2 | `HANDOFF-2026-04-30.md` (precedence 0, current-state ground truth), `BUILD-PLAN.md` (precedence 4) |
| **Total** | **6 docs** | |

All 6 classifications loaded with `confidence: high`, `manifest_override: true`. No `UNKNOWN`-confidence-low docs. No untagged docs.

---

## Decisions extracted

- **8 ADR decisions** synthesized into `decisions.md`, all from `phase-1-design-decisions.md`.
- **7 LOCKED** (ADR-002 through ADR-008): prototype path, confirmed implemented per HANDOFF.
- **1 PROVISIONAL** (ADR-001 — API architecture): production deferred to Mohammad's design kickoff per orchestrator instruction. Treated as provisional, not LOCKED, even though the source ADR document marks all eight as locked. Implementation discipline (route handlers as pure functions in `lib/api/*`) is binding even though the topology is provisional.

Decisions index:
- ADR-001 — API architecture: Next.js `/api/*` routes (PROVISIONAL)
- ADR-002 — Identity: CWID-canonical with `cwid_aliases` (LOCKED)
- ADR-003 — URL strategy: slug-primary HTML, CWID-keyed API (LOCKED)
- ADR-004 — Departed faculty: strict delete + 60-day soft-delete retention (LOCKED)
- ADR-005 — Daily refresh failure modes: per-source independent + ED-first abort cascade (LOCKED)
- ADR-006 — DynamoDB integration: minimal-projection ETL into MySQL (LOCKED)
- ADR-007 — Search engine: OpenSearch, embeddings deferred (LOCKED)
- ADR-008 — Render strategy: Next.js ISR with on-demand revalidation (LOCKED)

---

## Requirements extracted

19 requirements synthesized into `requirements.md`. IDs:

- REQ-platform-replace-vivo
- REQ-public-scholars-site
- REQ-scholar-api
- REQ-source-system-integrations
- REQ-self-edit-overview
- REQ-vivo-decommission
- REQ-profile-page
- REQ-publications-ranking *(competing-variants — see WARNINGS)*
- REQ-search
- REQ-role-model
- REQ-home-page
- REQ-topic-detail-page (new in design spec v1.7.1)
- REQ-department-detail-page (new in design spec v1.7.1)
- REQ-browse-hub-page (new in design spec v1.7.1)
- REQ-about-page (new in design spec v1.7.1)
- REQ-support-page
- REQ-sitemap-and-seo
- REQ-analytics-instrumentation
- REQ-cross-cutting

Plus a consolidated out-of-scope listing and a calibration TODO table (14 open calibration items from functional spec + design spec).

---

## Constraints extracted

`constraints.md` contains 28 constraints across 4 types:

| Type | Count |
|---|---|
| nfr | 2 (mobile-responsive, daily-refresh-cadence) |
| schema | 5 (cwid-canonical, soft-delete, slug-history, refresh-tracking, codes-as-join-keys) |
| api-contract | 5 (cwid-keyed API, OpenAPI artifact, search proxy, revalidate webhook, health endpoint) |
| protocol | 16 (search field weights, authorship weighting, min-evidence threshold, pagination 20/page LOCKED, schema-change protocol, algorithmic surface guidelines, citation format, palette/typography/header design tokens, ED field rules, COI scope, ETL pipeline rules, runtime-MySQL-only, source-system connection notes) |

Plus a pre-build open-items list (6 items) and an implementation-lessons section (4 items carried forward from build experience).

---

## Context topics

`context.md` covers 11 topics:
- Project background and business case (charter)
- Current state — what exists today (HANDOFF as factual ground truth)
- Build phase status (Phases 0-4 SHIPPED; 5-7 NOT YET DONE)
- Spec amendment during build (COI Disclosures)
- Mohammad's preliminary lean (production architecture)
- Handoff target audience and recommendations
- Open items requiring stakeholder input
- Methodology page (must-exist-before-launch)
- Conversations in flight (4 with target dates)
- Component-render logging (operational debugging)
- Phase 2+ deferred features (per Mohammad's slides 16-19 + design spec carve-outs)
- Working agreements during build
- Spec-precedence stack (project-internal)

---

## Conflicts: 0 blockers, 1 competing-variant, 8 auto-resolved

**Blockers:** none. No LOCKED-vs-LOCKED ADR contradictions. No UNKNOWN-confidence-low docs. No content cycles in cross-ref graph (BUILD-PLAN ↔ HANDOFF documentation loop is benign — see INFO entry in conflicts report).

**Competing variants (1):** REQ-publications-ranking has two divergent algorithm specifications between functional spec (Variant A — additive arithmetic, single recency curve, profile-only) and design spec v1.7.1 (Variant B — multiplicative, surface-specific recency curves, scholar-attributed authorship-position filter, multiple new surfaces). Both variants preserved in CONFLICTS report; user must choose before routing. The HANDOFF current-state line "publications (with both ranking formulas + WCM coauthor chips)" does not disambiguate which formula(s) are implemented.

**Auto-resolved (8):** ADR-006 wins over design spec on runtime DAL (locked-ADR > SPEC); design spec v1.7.1 wins over functional spec on home-page composition / role model / new page types (locked UI/UX contract per orchestrator); charter "microservices required" superseded by ADR-001 provisional; HANDOFF spec amendment for COI Disclosures aligns with design spec v1.7.1; pagination locked in functional spec compatible with design spec rendering pattern; documentation cross-ref loop benign.

See `.planning/INGEST-CONFLICTS.md` for full detail.

---

## NEW SCOPE being added on top of ingested docs

**Per user instruction, the orchestrator will inject one additional requirement during the new-project routing step that is NOT yet present in the ingested intel:**

- **Headshot API integration from Enterprise Directory.** Port the headshot-fetching pattern from the sibling project `ReCiter-Publication-Manager`. Charter mentions headshots only as a profile field; design spec v1.7.1 references headshots throughout (sidebar photo, search result rows, scholar chips, scholar-card grids on home and topic pages); HANDOFF "What works" lists profile pages rendering "header" but does not enumerate headshots specifically. The new scope requirement covers the integration surface — auth, cache strategy, fallback (initials avatar already used in design spec component spec when no image available), refresh cadence — and is treated as **additive Phase 1+ work**, not in any of the 8 ratified ADRs.

This requirement should be added to `requirements.md` as `REQ-headshot-api-integration` during routing, sourced explicitly from user instruction (not from any ingested doc). It does not need to be added to intel here; the orchestrator handles it in the next step.

---

## Pointers for downstream consumers

- Intel files (per-type):
  - `.planning/intel/decisions.md` — 8 ADR-derived decisions with locked/provisional status
  - `.planning/intel/requirements.md` — 19 requirements + calibration TODO table + out-of-scope list
  - `.planning/intel/constraints.md` — 28 constraints across nfr / schema / api-contract / protocol + pre-build open items + implementation lessons
  - `.planning/intel/context.md` — business case, current state, phase status, deferred features, working agreements
- Conflicts report:
  - `.planning/INGEST-CONFLICTS.md` — three-bucket plain-text per `references/doc-conflict-engine.md`
- Source classifications:
  - `.planning/intel/classifications/*.json` — 6 per-doc classifications, all manifest-overridden, all high-confidence

---

## Status

**STATUS: AWAITING USER** — competing variants in REQ-publications-ranking need resolution before downstream routing can produce a coherent ranking-algorithm requirement. All other intel is safe to consume.

If the user resolves the variants by saying "use design spec v1.7.1 across the board" (consistent with the orchestrator's locked-UI/UX-contract instruction), routing can proceed treating Variant B as the locked target with Variant A captured as historical context only. That is the strongest implicit signal in the orchestrator's instructions but the variant divergence is deep enough (different weight scales, multiplicative vs additive composition, additional algorithmic surfaces) that the synthesizer surfaces it rather than silently picks.
