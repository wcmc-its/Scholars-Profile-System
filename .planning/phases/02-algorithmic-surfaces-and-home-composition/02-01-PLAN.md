---
phase: 02-algorithmic-surfaces-and-home-composition
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/globals.css
  - components/ui/scroll-area.tsx
  - components/ui/skeleton.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements: [RANKING-01, RANKING-02, RANKING-03, HOME-02, HOME-03]
must_haves:
  truths:
    - "Tailwind utility text-sm renders at 13px not 14px"
    - "Tailwind utility text-base renders at 15px not 16px"
    - "Tailwind utility text-4xl renders at 44px not 36px"
    - "CSS variable --space-3 is declared at 12px in app/globals.css"
    - "CSS variable --color-accent-slate equals #2c4f6e in app/globals.css"
    - "components/ui/scroll-area.tsx and components/ui/skeleton.tsx exist with shadcn-generated content"
  artifacts:
    - path: "app/globals.css"
      provides: "Phase 2 design tokens (typography scale 13/15/18/44, weights 400/600, spacing including --space-3:12px, Slate accent, Charter font stack, layout maxima)"
      contains: "--space-3: 12px"
    - path: "components/ui/scroll-area.tsx"
      provides: "shadcn ScrollArea primitive for carousel"
    - path: "components/ui/skeleton.tsx"
      provides: "shadcn Skeleton primitive for loading states"
  key_links:
    - from: "Tailwind 4 @theme inline block"
      to: "All Phase 2 components using text-sm/text-base/text-lg/text-4xl/font-semibold"
      via: "CSS variable resolution at build time"
      pattern: "@theme inline"
---

<objective>
Wave 0 prerequisite. Port the design tokens (typography 4 sizes / 2 weights, spacing scale including the named 12px exception, color tokens for Slate accent + Cornell red, font stacks for Inter and Charter/Tiempos) from `.planning/sketches/themes/default.css` into `app/globals.css`, and install the two shadcn primitives Phase 2 components depend on (`scroll-area`, `skeleton`).

Purpose: every Phase 2 component (Recent contributions cards, Top scholars chips, Browse grid, Selected research carousel, methodology page) is written assuming `text-sm = 13px`, `text-base = 15px`, `text-lg = 18px`, `text-4xl = 44px`, `--space-3 = 12px`, and `--color-accent-slate = #2c4f6e`. Without this port, components inherit Tailwind 4 defaults (text-sm = 14px, text-base = 16px, text-4xl = 36px) and look subtly wrong throughout the phase. Listed as Wave 0 prerequisite in 02-RESEARCH.md §Pitfall 2 and §Recommended Wave Groupings.

Output: `app/globals.css` with extended `:root` and `@theme inline` blocks; `components/ui/scroll-area.tsx` and `components/ui/skeleton.tsx` from shadcn registry; `package.json` updated with `@radix-ui/react-scroll-area` dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-algorithmic-surfaces-and-home-composition/02-CONTEXT.md
@.planning/phases/02-algorithmic-surfaces-and-home-composition/02-UI-SPEC.md
@.planning/phases/02-algorithmic-surfaces-and-home-composition/02-RESEARCH.md
@.planning/phases/02-algorithmic-surfaces-and-home-composition/02-PATTERNS.md
@app/globals.css
@.planning/sketches/themes/default.css

<interfaces>
<!-- Token block to copy verbatim from default.css. Phase 2 components will read these via Tailwind utilities and direct var() references. -->

From `.planning/sketches/themes/default.css` (lines 4–80, the relevant block to port):
```css
:root {
  /* Typography */
  --text-sm: 13px;
  --text-base: 15px;
  --text-lg: 18px;
  --text-4xl: 44px;
  --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  --font-serif: 'Charter', 'Tiempos', 'Georgia', serif;
  /* Weights — only normal and semibold are used in Phase 2 per UI-SPEC checker audit */
  --weight-normal: 400;
  --weight-semibold: 600;
  /* Spacing scale (8-point grid + named 12px exception) */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;
  --space-12: 48px;
  --space-16: 64px;
  /* Color */
  --color-primary-cornell-red: #B31B1B;
  --color-accent-slate: #2c4f6e;
  /* Layout */
  --max-content: 1100px;
  --max-narrow: 720px;
  --header-h: 60px;
}
```

shadcn install commands (from research §To Be Added):
```bash
npx shadcn@latest add scroll-area
npx shadcn@latest add skeleton
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port design tokens into app/globals.css</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (current state — has shadcn color tokens in :root and @theme inline block lines 51-74; need to extend without removing existing content)
    - .planning/sketches/themes/default.css (token source — verify exact values before copying)
    - .planning/phases/02-algorithmic-surfaces-and-home-composition/02-UI-SPEC.md §Typography, §Spacing Scale, §Color (the contract these tokens must satisfy)
    - .planning/phases/02-algorithmic-surfaces-and-home-composition/02-RESEARCH.md §Pitfall 2 (why this is Wave 0; warning signs to avoid)
    - .planning/phases/02-algorithmic-surfaces-and-home-composition/02-PATTERNS.md "app/globals.css (MODIFIED — port design tokens from sketch theme)" section (exact tokens to add)
  </read_first>
  <behavior>
    - Tailwind utility `text-sm` resolves to 13px (not Tailwind 4 default 14px) after the port
    - Tailwind utility `text-base` resolves to 15px (not 16px) after the port
    - Tailwind utility `text-4xl` resolves to 44px (not 36px) after the port
    - Tailwind utility `text-lg` resolves to 18px (matches default but lock the value)
    - CSS var `var(--space-3)` evaluates to 12px in any component
    - CSS var `var(--color-accent-slate)` evaluates to `#2c4f6e`
    - CSS var `var(--color-primary-cornell-red)` evaluates to `#B31B1B`
    - Existing shadcn color tokens (`--background`, `--foreground`, `--card`, `--muted-foreground`, `--border`, etc.) are preserved unchanged
    - The Tailwind 4 `@theme inline` block exposes the new typography size tokens so utility classes remap
  </behavior>
  <action>
    Edit `app/globals.css`. Make TWO additive changes — do not remove or rewrite existing shadcn color tokens.

    1. In the existing `:root { ... }` block, append these tokens (verbatim values):
    ```css
    /* === Phase 2 design tokens (ported from .planning/sketches/themes/default.css) === */
    /* Typography */
    --text-sm: 13px;
    --text-base: 15px;
    --text-lg: 18px;
    --text-4xl: 44px;
    --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    --font-serif: 'Charter', 'Tiempos', 'Georgia', serif;
    /* Weights (only normal + semibold used in Phase 2 per UI-SPEC checker) */
    --weight-normal: 400;
    --weight-semibold: 600;
    /* Spacing — 8-point grid with named 12px exception */
    --space-1: 4px;
    --space-2: 8px;
    --space-3: 12px;
    --space-4: 16px;
    --space-6: 24px;
    --space-8: 32px;
    --space-12: 48px;
    --space-16: 64px;
    /* Color (locked by design spec v1.7.1) */
    --color-primary-cornell-red: #B31B1B;
    --color-accent-slate: #2c4f6e;
    /* Layout */
    --max-content: 1100px;
    --max-narrow: 720px;
    --header-h: 60px;
    ```

    2. In the existing `@theme inline { ... }` block, append remappings so Tailwind 4 utilities resolve to the new values:
    ```css
    /* Phase 2 — remap Tailwind size/weight utilities to design-spec tokens */
    --text-sm: var(--text-sm);
    --text-base: var(--text-base);
    --text-lg: var(--text-lg);
    --text-4xl: var(--text-4xl);
    --font-sans: var(--font-sans);
    --font-serif: var(--font-serif);
    --font-weight-normal: var(--weight-normal);
    --font-weight-semibold: var(--weight-semibold);
    ```

    Do NOT add `--weight-medium` or `--weight-bold` — UI-SPEC checker audit confirms Phase 2 uses ONLY weights 400 and 600 (see PATTERNS.md). Do NOT remove or modify existing shadcn color tokens (`--background`, `--foreground`, `--card`, `--muted-foreground`, `--border`, etc.) — they continue to drive shadcn primitives. Do NOT introduce a `tailwind.config.js` — Tailwind 4 reads `@theme` from CSS, not JS config.

    The `--space-N` scale uses the same numeric naming Tailwind 4 already uses for its spacing scale (gap-3, p-3, etc.) — Tailwind 4 reads spacing from `:root` automatically when a `--space-N` token is declared. No additional `@theme` remap needed for spacing.
  </action>
  <verify>
    <automated>grep -E "^\s*--space-3:\s*12px" app/globals.css | grep -v "^#" | wc -l | tr -d ' ' | grep -q "^[1-9]" && grep -E "^\s*--text-sm:\s*13px" app/globals.css | grep -v "^#" | wc -l | tr -d ' ' | grep -q "^[1-9]" && grep -E "^\s*--text-base:\s*15px" app/globals.css | grep -v "^#" | wc -l | tr -d ' ' | grep -q "^[1-9]" && grep -E "^\s*--text-4xl:\s*44px" app/globals.css | grep -v "^#" | wc -l | tr -d ' ' | grep -q "^[1-9]" && grep -E "^\s*--color-accent-slate:\s*#2c4f6e" app/globals.css | grep -v "^#" | wc -l | tr -d ' ' | grep -q "^[1-9]" && grep -E "^\s*--weight-semibold:\s*600" app/globals.css | grep -v "^#" | wc -l | tr -d ' ' | grep -q "^[1-9]" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\s*--space-3:\s*12px" app/globals.css` returns at least one match
    - `grep -E "^\s*--text-sm:\s*13px" app/globals.css` returns at least one match
    - `grep -E "^\s*--text-base:\s*15px" app/globals.css` returns at least one match
    - `grep -E "^\s*--text-4xl:\s*44px" app/globals.css` returns at least one match
    - `grep -E "^\s*--text-lg:\s*18px" app/globals.css` returns at least one match
    - `grep -E "^\s*--color-accent-slate:\s*#2c4f6e" app/globals.css` returns at least one match
    - `grep -E "^\s*--color-primary-cornell-red:\s*#B31B1B" app/globals.css` returns at least one match
    - `grep -E "^\s*--weight-semibold:\s*600" app/globals.css` returns at least one match
    - `grep -E "--font-sans:.*'Inter'" app/globals.css` returns at least one match (Inter must be first in the stack)
    - `grep -E "--font-serif:.*'Charter'" app/globals.css` returns at least one match
    - `grep -E "^\s*--background:" app/globals.css` STILL returns at least one match (existing shadcn tokens preserved)
    - `npm run typecheck` exits 0
    - `npm run lint -- app/globals.css` exits 0 (or 'no lint config for css' is acceptable)
  </acceptance_criteria>
  <done>All token grep checks pass; existing shadcn color tokens remain present; typecheck still passes (no TS code touched, just CSS — but typecheck verifies nothing accidentally regressed in adjacent files).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install shadcn scroll-area and skeleton primitives</name>
  <files>components/ui/scroll-area.tsx, components/ui/skeleton.tsx, package.json, package-lock.json</files>
  <read_first>
    - components/ui/avatar.tsx (existing shadcn primitive — verify the convention: imports `cn` from `@/lib/utils`, uses Radix UI primitive, named export only)
    - components.json (shadcn config — verifies `aliases.ui = "@/components/ui"` so installs land in the right place)
    - .planning/phases/02-algorithmic-surfaces-and-home-composition/02-UI-SPEC.md §Component Inventory and §Registry Safety (declares scroll-area + skeleton as the two new shadcn primitives this phase needs)
    - .planning/phases/02-algorithmic-surfaces-and-home-composition/02-RESEARCH.md §To Be Added (gives the two install commands)
  </read_first>
  <behavior>
    - `components/ui/scroll-area.tsx` exists, exports a `ScrollArea` component, imports `cn` from `@/lib/utils`
    - `components/ui/skeleton.tsx` exists, exports a `Skeleton` component, imports `cn` from `@/lib/utils`
    - `package.json` has `@radix-ui/react-scroll-area` in `dependencies` (skeleton uses no extra Radix dep — it is just a styled div)
    - `npm install` (or implicit on shadcn add) leaves a clean lockfile
  </behavior>
  <action>
    Run from project root:
    ```bash
    npx shadcn@latest add scroll-area
    npx shadcn@latest add skeleton
    ```

    The shadcn CLI uses `components.json` to determine the install location (`@/components/ui`). Do NOT hand-write either file — accept the registry output verbatim. Do NOT modify the generated files even if formatting differs from project preference; matching upstream shadcn output is the established pattern (see `components/ui/avatar.tsx`, `components/ui/card.tsx`).

    If the CLI prompts for confirmation, accept the default. If it asks about overwriting existing files, abort the install — it means the file already exists, in which case verify it has the expected `ScrollArea` / `Skeleton` export and skip the install.

    After install, verify:
    ```bash
    grep -q 'export.*ScrollArea' components/ui/scroll-area.tsx
    grep -q 'export.*Skeleton' components/ui/skeleton.tsx
    grep -q '@radix-ui/react-scroll-area' package.json
    ```

    Do NOT install any other shadcn components. The phase only declares these two.
  </action>
  <verify>
    <automated>test -f components/ui/scroll-area.tsx && test -f components/ui/skeleton.tsx && grep -q 'export.*ScrollArea' components/ui/scroll-area.tsx && grep -q 'export.*Skeleton' components/ui/skeleton.tsx && grep -q '@radix-ui/react-scroll-area' package.json && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `components/ui/scroll-area.tsx` exists
    - File `components/ui/skeleton.tsx` exists
    - `grep -E "export.*ScrollArea" components/ui/scroll-area.tsx` returns at least one match
    - `grep -E "export.*Skeleton" components/ui/skeleton.tsx` returns at least one match
    - `grep -E '"@radix-ui/react-scroll-area":' package.json` returns at least one match
    - `grep -E "from \"@/lib/utils\"" components/ui/scroll-area.tsx` returns at least one match (uses project `cn` helper)
    - `npm run typecheck` exits 0 (TS catches any import resolution failure)
    - `npm run lint -- components/ui/scroll-area.tsx components/ui/skeleton.tsx` exits 0
  </acceptance_criteria>
  <done>Both primitives present at `components/ui/`, exports correctly named, Radix dep added to package.json, typecheck passes, lint passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Untrusted package source crosses here (shadcn CLI fetches from npm + shadcn registry) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Tailwind 4 `@theme` block (CSS injection if values misnamed) | mitigate | Hard-coded literal values copied from `default.css`; no user input flows into globals.css; PR review catches typos |
| T-02-01-02 | Information disclosure | Design tokens leaked into bundle | accept | Tokens are public (color, sizes); no PII or credentials; this IS user-visible data |
| T-02-01-03 | Spoofing | Malicious shadcn registry returns hostile component | accept | shadcn registry is trusted upstream (audited Radix + minimal styled div); same supply-chain trust posture as Milestone 1 component installs (avatar, badge, button, card, etc.); package-lock.json captures sub-deps for reproducibility |
| T-02-01-04 | Tampering of CSS at build time | Wrong token values silently override Tailwind defaults | mitigate | Acceptance criteria grep for exact token values; downstream component tasks visually verify against sketch 003 in Wave 4 |

ASVS L1 categories applied: V14 (Configuration — token declaration is config). V5/V7/V8 not applicable (no input validation, no logging, no data protection in this plan).
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint` exits 0 for changed files
- All grep gates in acceptance criteria pass
- Existing shadcn color tokens (`--background`, `--foreground`, `--card`, `--muted-foreground`, `--border`) still present in `app/globals.css`
</verification>

<success_criteria>
- All `must_haves.truths` verifiable via grep against `app/globals.css` and the two new component files
- `npm run typecheck` and `npm run lint` exit 0
- No regression in existing shadcn primitives (`components/ui/avatar.tsx` etc. unchanged)
- Wave 1 (Variant B core) and Wave 3 (component implementation) plans can execute in parallel against this token foundation without conflict
</success_criteria>

<output>
After completion, create `.planning/phases/02-algorithmic-surfaces-and-home-composition/02-01-SUMMARY.md` documenting: tokens added, dependencies pulled in (radix-scroll-area version), any divergence from the sketch theme values (none expected).
</output>
