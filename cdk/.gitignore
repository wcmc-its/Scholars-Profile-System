*.js
*.d.ts
node_modules

# CDK asset staging directory
.cdk.staging
cdk.out
cdk.out.*

# Jest coverage output
coverage

# CDK context cache (account-specific AZ / AMI lookups; env-agnostic synth doesn't need it tracked)
cdk.context.json
