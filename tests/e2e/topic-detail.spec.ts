import { test, expect } from "@playwright/test";

test.describe("Topic detail page Layout B", () => {
  test.fixme("hero, top scholars row, recent highlights, subtopic rail, publication feed all render", async ({ page }) => {
    // Plan 07 will replace fixme with implementation
  });
  test.fixme("sort dropdown changes feed order to Most cited", async ({ page }) => {});
  test.fixme("Curated tag appears for By impact and Curated sorts", async ({ page }) => {});
  test.fixme("subtopic click filters feed and updates heading to subtopic label", async ({ page }) => {});
  test.fixme("View all N scholars link navigates to /search?topic={slug}&tab=people", async ({ page }) => {});
});
