/**
 * /docs chrome (v0). The docs site is a single comprehensive page that owns its
 * own two-column (scroll-spy sidebar + content) layout, rendered inside the
 * shared public header/footer from app/(public)/layout.tsx. Nothing to add here.
 */
export default function DocsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
